Backtest Performance Optimization & Project Organization

1. Performance Optimization (~720x Speedup):
- Implemented `PriceMatrix` in `src/utils/price_matrix.py` for pre-loaded OHLCV data.
- Added `--fast` flag to `run_optimization.py` to bypass per-ticker API calls and inline risk management.
- Enabled `--fast` mode in `run_all_backtests.py` for high-speed multi-year batch backtesting.

2. New Ablation Benchmark:
- Created `run_selection_only_backtest.py` to evaluate Selection Layer performance without LLM allocators.
- Integrated `PriceMatrix` for end-to-end efficiency in long-term runs.

3. Project Organization & Cleanup:
- Relocated debug/utility scripts to `model_strategy_selection/`.
- Removed legacy and redundant root scripts (`plot_backtest.py`, `run_stress_tests.py`, `run_evaluations.ps1`).
- Updated `.gitignore` to track model selection results.

4. Environment Compatibility:
- Fixed `sys.path` in `run_deepseek_experiment.py` for robust cross-platform/cloud execution.

Verified 10-day backtest runs in ~1.5s (prev ~20min).
